[SERVER]
# Specify here the interface of the webserver. Defining a domain will only allow 
# querys to the given address and response to all other querys with 403 Forbidden
# (Note: This will block querys using IP adresses as well).
host = 
broadcast_ip = 
port =  
domain = 
